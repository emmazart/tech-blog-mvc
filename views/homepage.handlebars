<section>
    <h2>Recent Posts</h2>
    {{#each blogposts as |post|}}
    <article>
        <h3>{{post.title}}</h3>
        <p>Posted by: {{post.user.username}}</p>
    </article>
    {{/each}}
</section>